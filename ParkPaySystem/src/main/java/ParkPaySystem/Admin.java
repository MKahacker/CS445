package ParkPaySystem;

public class Admin {
    /*Park[] listOfParks;

    public Admin(Park[] parks){
        listOfParks = parks;
    }

    public String listParks(){
        String allTheParks = "";
        for(Park onePark : this.listOfParks){
            allTheParks = allTheParks + onePark.viewInformation() + "\n";
        }
        return allTheParks;
    }

    public void createPark(String name, String location){
        Park newPark = new Park(101, name, 12.00, new Comment[0],location);
        this.listOfParks = increaseListSize();
        this.listOfParks[this.listOfParks.length-1] = newPark;
    }

    public Park[] increaseListSize(){
        Park[] temp = new Park[this.listOfParks.length+1];
        for(int i = 0; i < this.listOfParks.length; i++){
            temp[i] = this.listOfParks[i];
        }
        return temp;
    }*/
}
